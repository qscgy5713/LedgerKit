/ledgerkit
/ledgerkit-server

# Capacitor/Android tooling — reproducible from mobile/package.json and
# `npx cap add android`; android/ has its own .gitignore for build output.
/mobile/node_modules

# a real journal is personal financial data — only the example belongs in git.
*.journal
!example.journal

# CSV import dedupe state and personal bank exports
*.ledgerkit-import-state
*.csv

# goreleaser's build output — reproducible from .goreleaser.yaml, never committed.
/dist
